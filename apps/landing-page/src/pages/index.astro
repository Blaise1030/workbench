---
import "../styles/global.css";
import { CHANGELOG_URL, LATEST_RELEASE_JSON_URL, LATEST_RELEASE_PAGE_URL } from "../constants/releases";
import claudeSymbolSvg from "../../../desktop/src/assets/claude-ai-symbol.svg?raw";

/** Same as `apps/desktop/src/constants/appMeta.ts` — threads panel header title */
const APP_DISPLAY_NAME = "workbench";

interface ReleaseManifest {
	version?: string;
	downloads?: Record<string, { url: string; filename: string }>;
}

let releaseManifest: ReleaseManifest = {};
try {
	const res = await fetch(LATEST_RELEASE_JSON_URL);
	if (res.ok) releaseManifest = await res.json() as ReleaseManifest;
} catch { /* use fallback */ }

const ssrDownloadUrl = releaseManifest.downloads?.mac?.url ?? LATEST_RELEASE_PAGE_URL;
const ssrVersion = releaseManifest.version;

function pathDFromSvg(svg: string): string {
	const m = svg.match(/\sd="([^"]+)"/);
	if (!m?.[1]) {
		throw new Error("claude-ai-symbol.svg: expected a single path with d attribute");
	}
	return m[1];
}

const claudePathD = pathDFromSvg(claudeSymbolSvg);
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/png" href="/workbench-logo.png" />
		<link rel="apple-touch-icon" href="/workbench-logo.png" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="workbench — a terminal built for the multi-agent era. Run Claude Code, Gemini CLI, Cursor Agent, and more side by side, each isolated on its own worktree. Bring your own subscription."
		/>
		<title>workbench — Terminal for the multi-agent era</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Bungee+Shade&display=swap"
			rel="stylesheet"
		/>
	</head>
	<body class="relative overflow-x-hidden">
		<header class="bg-transparent">
			<div class="mx-auto flex max-w-5xl items-center px-6 py-2.5">
				<a href="/" class="flex min-w-0 flex-1 items-center gap-1 overflow-hidden" aria-label={`${APP_DISPLAY_NAME} home`}>
					<h2
						class="relative m-0 flex min-w-0 flex-1 flex-nowrap items-center gap-1 overflow-hidden p-0 text-zinc-900"
						data-testid="thread-sidebar-brand"
					>
						<img
							src="/workbench-logo.png"
							alt=""
							width="32"
							height="32"
							loading="eager"
							decoding="async"
							draggable="false"
							class="pointer-events-none size-8 shrink-0 select-none rounded-lg object-cover"
							aria-hidden="true"
							data-testid="thread-sidebar-logo"
						/>
						<span
							class="font-app-brand-title block min-w-0 truncate text-base uppercase leading-none text-zinc-900"
							data-testid="app-title-a11y"
						>
							{APP_DISPLAY_NAME}
						</span>
						<span
							class="shadow-xs ms-0.5 mt-0.5 inline-flex h-3.5 min-h-0 shrink-0 items-center justify-center rounded-sm border border-zinc-200 bg-transparent px-1 py-0 text-[8px] font-semibold uppercase leading-none tracking-wide text-zinc-900"
							data-testid="thread-sidebar-alpha-badge"
						>
							Alpha
						</span>
					</h2>
				</a>
			</div>
		</header>

		<main class="px-2">
			<section class="scroll-scale-in mx-auto max-w-5xl px-6 pb-16 pt-16 text-center md:pb-20 md:pt-20" aria-labelledby="hero-heading">
				<h1 id="hero-heading" class="mx-auto max-w-5xl text-balance text-4xl font-medium leading-[1.1] tracking-tight text-zinc-900 md:text-5xl lg:text-[3.25rem]">
					Run every AI agent. No conflicts.
				</h1>
				<p class="mx-auto mt-5 max-w-3xl text-lg text-zinc-600">
					workbench gives Claude Code, Gemini CLI, and Cursor Agent their own isolated git worktree — so they work in parallel without stepping on each other. Bring your own subscription.
				</p>
				<div id="download" class="mt-10 flex flex-col items-center gap-2">
					<a
						id="download-btn"
						href={ssrDownloadUrl}
						class="inline-flex items-center gap-2 rounded-full bg-zinc-900 px-7 py-3.5 text-sm font-semibold text-white shadow-lg shadow-zinc-900/15 transition hover:bg-zinc-800"
						rel="noopener noreferrer"
					>
						<svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
							<path
								d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.81-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"
							/>
						</svg>
						<span id="download-btn-label">Download for macOS — it's free</span>
					</a>
					<p id="download-version" class={`text-xs text-zinc-500${ssrVersion ? "" : " hidden"}`} aria-live="polite">
						{ssrVersion ? `Version ${ssrVersion}` : ""}
					</p>
				</div>
			</section>

			<section class="scroll-scale-in mx-auto max-w-5xl pb-12" aria-labelledby="preview-heading">
				<h2 id="preview-heading" class="sr-only">Product preview</h2>
				<div
					class="preview-scale-in flex flex-col overflow-hidden rounded-b-xl border border-zinc-200 bg-white shadow-lg shadow-zinc-900/5"
				>
					<div
						class="shrink-0 border-b border-zinc-200/80 bg-zinc-50/95 px-4 py-3 text-center text-xs text-zinc-600"
					>
						workbench — parallel agents, isolated worktrees, no conflicts.
					</div>
					<div class="group relative overflow-hidden rounded-b-[10px] bg-zinc-100">
						<img
							src="/workbench-preview-clean.png"
							alt="workbench desktop workspace showing sidebar worktrees, project tabs, and agent panel."
							loading="lazy"
							class="relative z-0 block h-auto w-full object-contain object-top"
							decoding="async"
							draggable="false"
						/>
						<img
							src="/workbench-preview-annotated.png"
							alt=""
							loading="lazy"
							class="pointer-events-none absolute inset-0 z-10 h-full w-full object-contain object-top opacity-0 transition-opacity duration-300 ease-out motion-reduce:transition-none group-hover:opacity-100"
							decoding="async"
							aria-hidden="true"
							draggable="false"
						/>
					</div>
				</div>
			</section>
		</main>

		<!-- Supported agents -->
		<section class="scroll-scale-in mx-auto max-w-5xl px-6 py-16" aria-labelledby="agents-heading">
			<p id="agents-heading" class="mb-8 text-center text-xs font-semibold uppercase tracking-widest text-zinc-400">Works with your agents</p>
			<div class="flex flex-wrap items-center justify-center gap-x-10 gap-y-5">
				{[
					{
						name: "Claude Code",
						viewBox: "0 0 1200 1200",
						path: claudePathD,
					},
					{
						name: "Gemini CLI",
						viewBox: "0 0 24 24",
						path: "M11.04 19.32Q12 21.51 12 24q0-2.49.93-4.68.96-2.19 2.58-3.81t3.81-2.55Q21.51 12 24 12q-2.49 0-4.68-.93a12.3 12.3 0 0 1-3.81-2.58 12.3 12.3 0 0 1-2.58-3.81Q12 2.49 12 0q0 2.49-.96 4.68-.93 2.19-2.55 3.81a12.3 12.3 0 0 1-3.81 2.58Q2.49 12 0 12q2.49 0 4.68.96 2.19.93 3.81 2.55t2.55 3.81",
					},
					{
						name: "Cursor Agent",
						viewBox: "0 0 24 24",
						path: "M11.503.131 1.891 5.678a.84.84 0 0 0-.42.726v11.188c0 .3.162.575.42.724l9.609 5.55a1 1 0 0 0 .998 0l9.61-5.55a.84.84 0 0 0-.42-.724V6.404a.84.84 0 0 0-.42-.726L12.497.131a1.01 1.01 0 0 0-.996 0M2.657 6.338h18.55c.263 0 .43.287.297.515L12.23 22.918c-.062.107-.229.064-.229-.06V12.335a.59.59 0 0 0-.295-.51l-9.11-5.257c-.109-.063-.064-.23.061-.23",
					},
					{
						name: "OpenAI Codex",
						viewBox: "0 0 24 24",
						path: "M22.2819 9.8211a5.9847 5.9847 0 0 0-.5157-4.9108 6.0462 6.0462 0 0 0-6.5098-2.9A6.0651 6.0651 0 0 0 4.9807 4.1818a5.9847 5.9847 0 0 0-3.9977 2.9 6.0462 6.0462 0 0 0 .7427 7.0966 5.98 5.98 0 0 0 .511 4.9107 6.051 6.051 0 0 0 6.5146 2.9001A5.9847 5.9847 0 0 0 13.2599 24a6.0557 6.0557 0 0 0 5.7718-4.2058 5.9894 5.9894 0 0 0 3.9977-2.9001 6.0557 6.0557 0 0 0-.7475-7.0729zm-9.022 12.6081a4.4755 4.4755 0 0 1-2.8764-1.0408l.1419-.0804 4.7783-2.7582a.7948.7948 0 0 0 .3927-.6813v-6.7369l2.02 1.1686a.071.071 0 0 1 .038.052v5.5826a4.504 4.504 0 0 1-4.4945 4.4944zm-9.6607-4.1254a4.4708 4.4708 0 0 1-.5346-3.0137l.142.0852 4.783 2.7582a.7712.7712 0 0 0 .7806 0l5.8428-3.3685v2.3324a.0804.0804 0 0 1-.0332.0615L9.74 19.9502a4.4992 4.4992 0 0 1-6.1408-1.6464zM2.3408 7.8956a4.485 4.485 0 0 1 2.3655-1.9728V11.6a.7664.7664 0 0 0 .3879.6765l5.8144 3.3543-2.0201 1.1685a.0757.0757 0 0 1-.071 0l-4.8303-2.7865A4.504 4.504 0 0 1 2.3408 7.872zm16.5963 3.8558L13.1038 8.364 15.1192 7.2a.0757.0757 0 0 1 .071 0l4.8303 2.7913a4.4944 4.4944 0 0 1-.6765 8.1042v-5.6772a.79.79 0 0 0-.407-.667zm2.0107-3.0231l-.142-.0852-4.7735-2.7818a.7759.7759 0 0 0-.7854 0L9.409 9.2297V6.8974a.0662.0662 0 0 1 .0284-.0615l4.8303-2.7866a4.4992 4.4992 0 0 1 6.6802 4.66zM8.3065 12.863l-2.02-1.1638a.0804.0804 0 0 1-.038-.0567V6.0742a4.4992 4.4992 0 0 1 7.3757-3.4537l-.142.0805L8.704 5.459a.7948.7948 0 0 0-.3927.6813zm1.0976-2.3654l2.602-1.4998 2.6069 1.4998v2.9994l-2.5974 1.4997-2.6067-1.4997Z",
					},
				].map(({ name, viewBox, path }) => (
					<div class="flex items-center gap-2 text-sm font-medium text-zinc-600">
						<svg
							class="h-5 w-5 shrink-0 text-zinc-500"
							viewBox={viewBox}
							fill="currentColor"
							aria-hidden="true"
						>
							<path d={path} />
						</svg>
						{name}
					</div>
				))}
			</div>
		</section>

		<!-- Divider -->
		<div class="mx-auto max-w-5xl px-6">
			<hr class="border-zinc-100" />
		</div>

		<!-- Feature bento grid -->
		<section class="scroll-scale-in mx-auto max-w-5xl px-6 py-20" aria-labelledby="features-heading">
			<h2 id="features-heading" class="sr-only">Features</h2>
			<div class="grid grid-cols-1 gap-4 md:grid-cols-2">

				<!-- Card 1: Parallel agents (large) -->
				<div class="flex flex-col overflow-hidden rounded-2xl border border-zinc-200 bg-zinc-50 p-7 shadow-sm shadow-zinc-900/5">
					<h3 class="text-base font-semibold text-zinc-900">Parallel agents, no chaos</h3>
					<p class="mt-1.5 text-sm leading-relaxed text-zinc-500">Open Claude Code on auth, Gemini CLI on the API, and Cursor on the UI — all at once. Each agent works on its own branch. No conflicts, no shared state.</p>
					<!-- TODO: replace with thread/terminal snippet screenshot -->
					<div class="mt-6 flex flex-1 items-center justify-center rounded-xl border border-dashed border-zinc-300 bg-zinc-100 px-4 py-10 text-xs text-zinc-400">
						screenshot: thread terminal snippet
					</div>
				</div>

				<!-- Card 2: Worktree isolation (large) -->
				<div class="flex flex-col overflow-hidden rounded-2xl border border-zinc-200 bg-[#f5f4ff] p-7 shadow-sm shadow-zinc-900/5">
					<h3 class="text-base font-semibold text-zinc-900">Each agent, its own worktree</h3>
					<p class="mt-1.5 text-sm leading-relaxed text-zinc-500">Every session gets an isolated git worktree. Changes stay separate until you're ready to review and merge. You decide what lands — not whoever wrote last.</p>
					<!-- TODO: replace with worktree switcher screenshot -->
					<div class="mt-6 flex flex-1 items-center justify-center rounded-xl border border-dashed border-violet-200 bg-violet-50 px-4 py-10 text-xs text-violet-300">
						screenshot: worktree switcher snippet
					</div>
				</div>

				<!-- Card 3: BYOK -->
				<div class="flex flex-col overflow-hidden rounded-2xl border border-zinc-200 bg-[#f0faf4] p-7 shadow-sm shadow-zinc-900/5">
					<h3 class="text-base font-semibold text-zinc-900">Bring your own subscription</h3>
					<p class="mt-1.5 text-sm leading-relaxed text-zinc-500">workbench doesn't charge on top of your API spend. Use the subscriptions you already have and pay only for what the models actually cost.</p>
					<!-- TODO: replace with hero/follow-up snippet screenshot -->
					<div class="mt-6 flex flex-1 items-center justify-center rounded-xl border border-dashed border-emerald-200 bg-emerald-50 px-4 py-10 text-xs text-emerald-300">
						screenshot: hero / follow-up snippet
					</div>
				</div>

				<!-- Card 4: Review & merge -->
				<div class="flex flex-col overflow-hidden rounded-2xl border border-zinc-200 bg-[#fdf8f0] p-7 shadow-sm shadow-zinc-900/5">
					<h3 class="text-base font-semibold text-zinc-900">Review before you merge</h3>
					<p class="mt-1.5 text-sm leading-relaxed text-zinc-500">A built-in diff viewer shows exactly what each agent changed. Compare across agents, pick the best result, and merge — without leaving the app.</p>
					<!-- TODO: replace with git diff snippet screenshot -->
					<div class="mt-6 flex flex-1 items-center justify-center rounded-xl border border-dashed border-amber-200 bg-amber-50 px-4 py-10 text-xs text-amber-300">
						screenshot: git diff snippet
					</div>
				</div>

			</div>
		</section>

		<!-- Divider -->
		<div class="mx-auto max-w-5xl px-6">
			<hr class="border-zinc-100" />
		</div>

		<!-- How it works -->
		<section class="scroll-scale-in mx-auto max-w-5xl px-6 py-20" aria-labelledby="how-heading">
			<h2 id="how-heading" class="mb-12 text-center text-2xl font-medium tracking-tight text-zinc-900">How it works</h2>
			<div class="grid gap-4 md:grid-cols-3">
				<div class="flex flex-col gap-3 rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm shadow-zinc-900/5">
					<span class="text-xs font-semibold uppercase tracking-widest text-zinc-400">01</span>
					<h3 class="text-sm font-semibold text-zinc-900">Open your project</h3>
					<p class="text-sm leading-relaxed text-zinc-500">Point workbench at any local repo. Your workspace is ready: file tree, git status, and terminal — all in one window.</p>
				</div>
				<div class="flex flex-col gap-3 rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm shadow-zinc-900/5">
					<span class="text-xs font-semibold uppercase tracking-widest text-zinc-400">02</span>
					<h3 class="text-sm font-semibold text-zinc-900">Spin up agents</h3>
					<p class="text-sm leading-relaxed text-zinc-500">Add Claude Code, Gemini CLI, or any agent as a thread. Each gets a dedicated worktree so they can run independently on the same codebase.</p>
				</div>
				<div class="flex flex-col gap-3 rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm shadow-zinc-900/5">
					<span class="text-xs font-semibold uppercase tracking-widest text-zinc-400">03</span>
					<h3 class="text-sm font-semibold text-zinc-900">Review and merge</h3>
					<p class="text-sm leading-relaxed text-zinc-500">Compare diffs across agents side by side. Pick the best result and merge — without ever leaving the app.</p>
				</div>
			</div>
		</section>

		<!-- Closing CTA -->
		<section class="scroll-scale-in mx-auto max-w-5xl px-6 py-20 text-center" aria-labelledby="cta-heading">
			<p class="mx-auto max-w-xl text-lg text-zinc-600">
				You're already using AI agents. workbench just lets you run more of them, without the mess.
			</p>
			<div class="mt-8 flex flex-col items-center gap-2">
				<a
					href={ssrDownloadUrl}
					class="inline-flex items-center gap-2 rounded-full bg-zinc-900 px-7 py-3.5 text-sm font-semibold text-white shadow-lg shadow-zinc-900/15 transition hover:bg-zinc-800"
					rel="noopener noreferrer"
				>
					Download workbench — it's free
				</a>
				<p class="text-xs text-zinc-400">No subscription required. Works on macOS, Windows, and Linux.</p>
			</div>
		</section>

		<!-- Divider -->
		<div class="mx-auto max-w-5xl px-6">
			<hr class="border-zinc-100" />
		</div>

		<footer class="border-t border-zinc-200/80 bg-white py-10">
			<div
				class="mx-auto flex max-w-5xl flex-col items-center gap-3 px-6 text-center text-sm text-zinc-500 md:flex-row md:items-center md:justify-between md:text-left"
			>
				<p class="m-0">© {new Date().getFullYear()} workbench</p>
				<a
					href={CHANGELOG_URL}
					class="font-medium text-zinc-600 underline decoration-zinc-300 underline-offset-4 transition hover:text-zinc-900 hover:decoration-zinc-500"
					target="_blank"
					rel="noopener noreferrer"
				>
					Changelog
				</a>
			</div>
		</footer>

		<!-- Top-level: must stay outside `.scroll-scale-in` (transform breaks positioning). Centering: global.css -->
		<dialog
			id="mac-gatekeeper-notice"
			class="z-[300] max-w-none bg-transparent shadow-none"
			aria-labelledby="mac-gatekeeper-title"
			aria-describedby="mac-gatekeeper-desc"
		>
			<div
				class="mac-gatekeeper-panel rounded-2xl border border-zinc-200 bg-white p-6 text-left shadow-2xl shadow-zinc-900/10"
			>
				<h2 id="mac-gatekeeper-title" class="text-base font-semibold text-zinc-900">
					workbench is downloading
				</h2>
				<p id="mac-gatekeeper-desc" class="mt-3 text-sm leading-relaxed text-zinc-600">
					The file should appear in your Downloads folder in a moment. We are not on the Apple Developer Program, so macOS may show a security warning the first time you open workbench. If it does, use System Settings (below) or in Finder right-click workbench and choose <span class="whitespace-nowrap">Open</span>.
				</p>
				<div class="mt-4 max-w-xl">
					<figure class="m-0 min-w-0 overflow-hidden rounded-xl border border-zinc-200 bg-zinc-50">
						<img
							src="/mac-gatekeeper-notice.png"
							alt="Screenshot: macOS System Settings → Security, message that workbench was blocked, with Open Anyway indicated."
							width="492"
							height="198"
							loading="lazy"
							decoding="async"
							class="block h-auto w-full"
						/>
						<figcaption class="border-t border-zinc-200 bg-white px-3 py-2 text-center text-[11px] font-medium leading-snug text-zinc-700">
							<span class="block">If macOS blocked the app: System Settings → Privacy &amp; Security → click <span class="whitespace-nowrap">Open Anyway</span>.</span>
							<span class="mt-1 block text-zinc-600">Or in Finder: right-click workbench → <span class="whitespace-nowrap">Open</span>.</span>
						</figcaption>
					</figure>
				</div>
				<form method="dialog" class="mt-6 flex justify-end">
					<button
						type="submit"
						class="rounded-full bg-zinc-900 px-5 py-2 text-sm font-semibold text-white transition hover:bg-zinc-800"
					>
						Got it
					</button>
				</form>
			</div>
		</dialog>

		<script>
			const preview = document.querySelector(".preview-scale-in");
			if (preview) {
				if ("IntersectionObserver" in window) {
					const io = new IntersectionObserver(
						(entries) => {
							for (const e of entries) {
								if (e.isIntersecting) {
									e.target.classList.add("preview-scale-in--visible");
									io.disconnect();
								}
							}
						},
						{ root: null, rootMargin: "0px 0px -8% 0px", threshold: 0.12 }
					);
					io.observe(preview);
				} else {
					preview.classList.add("preview-scale-in--visible");
				}
			}
		</script>

		<script>
			const revealSections = Array.from(document.querySelectorAll(".scroll-scale-in"));
			if (revealSections.length > 0) {
				if ("IntersectionObserver" in window) {
					const io = new IntersectionObserver(
						(entries) => {
							for (const entry of entries) {
								if (entry.isIntersecting) {
									entry.target.classList.add("scroll-scale-in--visible");
									io.unobserve(entry.target);
								}
							}
						},
						{ root: null, rootMargin: "0px 0px -10% 0px", threshold: 0.12 }
					);
					for (const section of revealSections) {
						io.observe(section);
					}
				} else {
					for (const section of revealSections) {
						section.classList.add("scroll-scale-in--visible");
					}
				}
			}
		</script>

		<script
			define:vars={{
				SSR_MANIFEST: releaseManifest,
				LATEST_RELEASE_PAGE_URL,
			}}
		>
			function pickPlatform() {
				const ua = navigator.userAgent.toLowerCase();
				if (/\bwin\b/.test(ua) || /windows/.test(ua)) return "windows";
				if (/macintosh|mac os x|mac os/.test(ua)) return "mac";
				if (/linux/.test(ua)) return "linux";
				return "mac";
			}

			function labelForPlatform(p) {
				if (p === "windows") return "Download for Windows — it's free";
				if (p === "linux") return "Download for Linux — it's free";
				return "Download for macOS — it's free";
			}

			/** Starts a file download without opening a new tab or navigating this page away. */
			function triggerDownloadSameTab(url) {
				const iframe = document.createElement("iframe");
				iframe.setAttribute("aria-hidden", "true");
				iframe.tabIndex = -1;
				iframe.style.cssText =
					"position:fixed;width:0;height:0;border:0;margin:0;padding:0;opacity:0;pointer-events:none;visibility:hidden";
				iframe.src = url;
				document.body.appendChild(iframe);
				window.setTimeout(() => iframe.remove(), 180_000);
			}

			(() => {
				const btn = document.getElementById("download-btn");
				const labelEl = document.getElementById("download-btn-label");
				const versionEl = document.getElementById("download-version");
				const macNotice = document.getElementById("mac-gatekeeper-notice");
				if (!btn || !labelEl) return;

				const platform = pickPlatform();
				labelEl.textContent = labelForPlatform(platform);

				const entry = SSR_MANIFEST?.downloads?.[platform];
				if (entry?.url) {
					btn.setAttribute("href", entry.url);
				} else {
					btn.setAttribute("href", LATEST_RELEASE_PAGE_URL);
				}

				if (versionEl && SSR_MANIFEST?.version) {
					versionEl.textContent = `Version ${SSR_MANIFEST.version}`;
					versionEl.classList.remove("hidden");
				}

				btn.addEventListener("click", (e) => {
					if (platform !== "mac") return;
					if (!(macNotice instanceof HTMLDialogElement)) return;
					e.preventDefault();
					const href = btn.getAttribute("href");
					macNotice.showModal();
					if (href) triggerDownloadSameTab(href);
				});
			})();
		</script>
	</body>
</html>
