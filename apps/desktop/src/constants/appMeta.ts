/** Product name shown in the threads panel header (keep in sync with index.html <title> when possible). */
export const APP_DISPLAY_NAME = "workbench";

/** Brand text in the threads panel header badge (uppercase). */
export const APP_BRAND_BADGE = "WORKBENCH";

/** Prerelease / channel label next to the brand badge. */
export const APP_PRERELEASE_BADGE = "Alpha";
